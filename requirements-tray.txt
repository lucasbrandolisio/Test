# Dipendenze extra per le parti grafiche di filesync (icona nella system
# tray + finestra di configurazione visuale 'filesync setup'). Il resto
# di filesync (sync, encrypt, lock/unlock, recover, protect...) NON ne ha
# bisogno: installale solo se vuoi usare la GUI.
#
#   pip install -r requirements.txt -r requirements-tray.txt
#
# 'filesync setup' da solo (senza l'icona tray) richiede solo 'keyring'
# oltre a Tkinter (incluso in Python): pystray/Pillow servono solo a
# 'filesync tray'.
#
# Su Linux minimale potrebbe servire anche il pacchetto di sistema
# 'python3-tk' (per le finestre di dialogo) e un ambiente desktop con
# supporto system tray (su GNOME serve l'estensione AppIndicator/
# KStatusNotifierItem). Su Windows e macOS, tkinter e' gia' incluso
# nell'installer ufficiale di Python.

pystray>=0.19
Pillow>=10.0
keyring>=24.0
